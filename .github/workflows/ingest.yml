# Place this at .github/workflows/ingest.yml in the PUBLIC aipp-weather-ingest repo.
# (Stored here with a flat name so it isn't an active workflow in the app repo.)
name: ingest

on:
  schedule:
    - cron: "0 7 * * *"    # ~00z ECMWF run, available by ~06-07z
    - cron: "0 19 * * *"   # ~12z ECMWF run, available by ~18-19z
  workflow_dispatch: {}
  repository_dispatch:
    types: [ingest]        # external free cron (cron-job.org / CF Worker)

concurrency:
  group: ingest
  cancel-in-progress: false

permissions:
  contents: write          # publish .db + latest.json as Release assets

jobs:
  ingest:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: System eccodes
        run: sudo apt-get update && sudo apt-get install -y libeccodes-tools
      - name: Python deps
        run: pip install ecmwf-opendata cfgrib xarray scipy numpy netCDF4 copernicusmarine
      - name: Run ingest
        continue-on-error: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # GITHUB_REPOSITORY is provided automatically by the runner.
          COPERNICUSMARINE_SERVICE_USERNAME: ${{ secrets.COPERNICUSMARINE_SERVICE_USERNAME }}
          COPERNICUSMARINE_SERVICE_PASSWORD: ${{ secrets.COPERNICUSMARINE_SERVICE_PASSWORD }}
        run: python ingest.py
